{{#paper-toolbar as |toolbar|}}
  {{#toolbar.tools}}
    <h3>Social Video</h3>
    {{#if creating}}
    {{else}}
      {{#paper-button onClick=(action "toggleThread")}}
        Create a Thread
      {{/paper-button}}
    {{/if}}
    <span class="flex"></span>
  {{/toolbar.tools}}
{{/paper-toolbar}}
{{#if creating}}
  {{create-thread createThread=(action "saveThread") cancelThread=(action "toggleThread")}}
{{/if}}
<div id="feed" class="feed">
  {{#each model as |thread|}}
    <div class="thread">
      <h2>{{thread.title}}</h2>
      {{#if thread.link}}
        <a href={{thread.link}}>{{thread.link}}</a>
      {{/if}}
      {{ivy-videojs controls=true src=thread.video}}
      {{#link-to 'thread' thread}}
        Open Thread
      {{/link-to}}
    </div>
  {{/each}}
</div>